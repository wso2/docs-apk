"use strict";
const check_1 = require("./check");
const contributor_1 = require("./contributor");
const time_1 = require("./time");
module.exports = (options, { cwd }) => {
    const isGitRepoValid = check_1.checkGitRepo(cwd);
    return {
        name: "git",
        async extendPageData(page) {
            if (isGitRepoValid && page._filePath) {
                const { transformer } = options;
                const { $lang } = page._computed;
                const createTimeStamp = await time_1.getCreatedTimeStamp(page._filePath);
                const updateTimeStamp = await time_1.getUpdatedTimeStamp(page._filePath);
                if (updateTimeStamp) {
                    const updatedTime = typeof transformer === "function"
                        ? transformer(updateTimeStamp, $lang)
                        : time_1.defaultTransformer(updateTimeStamp, $lang, options.timezone);
                    page.updateTime = updatedTime;
                    page.updateTimeStamp = updateTimeStamp;
                }
                if (createTimeStamp) {
                    const createTime = typeof transformer === "function"
                        ? transformer(createTimeStamp, $lang)
                        : time_1.defaultTransformer(createTimeStamp, $lang, options.timezone);
                    page.createTime = createTime;
                    page.createTimeStamp = createTimeStamp;
                }
                if (options.contributor !== false)
                    page.contributors = await contributor_1.getContributors(page._filePath);
            }
        },
    };
};
//# sourceMappingURL=index.js.map