import "./style/message.styl";
export default class Message {
    private containerElement;
    constructor();
    pop(text: string, duration?: number): void;
    close(messageElement: HTMLDivElement): void;
}
