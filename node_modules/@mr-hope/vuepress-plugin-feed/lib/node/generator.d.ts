import { Feed } from "./feed";
import type { Context, Page } from "@mr-hope/vuepress-types";
import type { FeedOptions, FeedInitOptions } from "../types";
export declare class FeedGenerator {
    private pages;
    private options;
    private context;
    /** feed 生成器 */
    feed: Feed;
    constructor(pages: Page[], options: FeedOptions, feedOption: FeedInitOptions, context: Context);
    addPages(): void;
    generateFeed(): Promise<void>;
}
