import type { CodeType, ReactCode, VueCode, NormalCode } from "./typings";
export declare const select: (node: Element | Document, selector: string) => HTMLElement[];
export interface DOMInitOption {
    container: HTMLElement;
    code: VueCode | NormalCode | ReactCode;
    codeType: CodeType;
    title: string;
}
export declare const initDom: ({ code, codeType, container, title, }: DOMInitOption) => void;
